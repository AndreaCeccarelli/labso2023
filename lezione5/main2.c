#include <stdio.h>

#include "palindrome.h"

int main (void) {

  printf ("palidrome (\"cat\") = %d\n", palindrome ("cat"));

  printf ("palidrome (\"noon\") = %d\n", palindrome ("noon"));

  return 0;
}

